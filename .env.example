# Kit (ConvertKit) newsletter — consumed by app/api/subscribe/route.ts
# Copy to .env.local and fill in. Also set these in Vercel project env vars.
CONVERTKIT_API_KEY=your_kit_public_api_key
CONVERTKIT_FORM_ID=your_seobaza_form_id
